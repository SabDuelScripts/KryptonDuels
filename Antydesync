local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local LP = Players.LocalPlayer

-- // THEME (FULL BLUE STYLE)
local BG         = Color3.fromRGB(8, 20, 50)       -- Dark blue background
local CARD_BG    = Color3.fromRGB(18, 45, 110)    -- Medium blue cards
local CARD_HOV   = Color3.fromRGB(25, 60, 150)    -- Lighter blue on hover
local BORDER     = Color3.fromRGB(70, 150, 255)   -- Bright blue border
local BORDER2    = Color3.fromRGB(100, 180, 255)  -- Light blue accent
local WHITE      = Color3.fromRGB(255, 255, 255)  -- White text
local DIM        = Color3.fromRGB(200, 220, 255)  -- Light blue text
local KB_BG      = Color3.fromRGB(12, 35, 85)     -- Dark blue buttons
local ACCENT     = Color3.fromRGB(70, 150, 255)   -- Bright blue accent

-- // STATE
local State = {
    autoBatToggled = false,
    hittingCooldown = false,
    guiVisible = true,
    mobileMode = false,
    savedGuiX = 0,
    savedGuiY = 0,
}

local Keys = {
    autoBat = Enum.KeyCode.X,
    autoBatType = "Keyboard", -- "Keyboard" or "Gamepad"
    guiHide = Enum.KeyCode.LeftControl,
}

local h, hrp = nil, nil

-- // CONFIG
local function saveConfig()
    local cfg = {
        autoBatKey = Keys.autoBat.Name,
        autoBatKeyType = Keys.autoBatType,
        mobileMode = State.mobileMode,
        guiPositionX = main and main.Position.X.Offset or State.savedGuiX,
        guiPositionY = main and main.Position.Y.Offset or State.savedGuiY,
    }
    pcall(function() 
        writefile("EnvyAutoBatDesyncConfig.json", HttpService:JSONEncode(cfg)) 
    end)
end

local function loadConfig()
    local hasFile = false
    pcall(function() hasFile = isfile("EnvyAutoBatDesyncConfig.json") end)
    if not hasFile then return end
    
    local ok, cfg = pcall(function() 
        return HttpService:JSONDecode(readfile("EnvyAutoBatDesyncConfig.json")) 
    end)
    if not ok or not cfg then return end
    
    if cfg.autoBatKey and Enum.KeyCode[cfg.autoBatKey] then
        Keys.autoBat = Enum.KeyCode[cfg.autoBatKey]
    end
    
    if cfg.autoBatKeyType then
        Keys.autoBatType = cfg.autoBatKeyType
    end
    
    if cfg.mobileMode ~= nil then 
        State.mobileMode = cfg.mobileMode 
    end
    
    -- Store saved position to apply after GUI is created
    State.savedGuiX = cfg.guiPositionX or 0
    State.savedGuiY = cfg.guiPositionY or 0
end

loadConfig()

-- // CLEANUP OLD GUI
for _, name in pairs({"EnvyAutoBatDesyncGUI", "MwvaneNewaBatDesyncGUI", "PhazeAutoBatDesyncGUI"}) do
    local old = game:GetService("CoreGui"):FindFirstChild(name)
    if old then old:Destroy() end
end

-- // GUI
local gui = Instance.new("ScreenGui")
gui.Name="EnvyAutoBatDesyncGUI"; gui.ResetOnSpawn=false; gui.DisplayOrder=10
gui.IgnoreGuiInset=true; gui.Parent=LP:WaitForChild("PlayerGui")

local main = Instance.new("Frame",gui)
main.Name="Main"; main.Size=UDim2.new(0,280,0,110); main.Position=UDim2.new(0.5,-140+State.savedGuiX,0.5,-55+State.savedGuiY)
main.BackgroundColor3=BG; main.BorderSizePixel=0; main.Active=true; main.ClipsDescendants=true
Instance.new("UICorner",main).CornerRadius=UDim.new(0,16)
local mainStroke = Instance.new("UIStroke",main); mainStroke.Color=BORDER; mainStroke.Thickness=2

-- // DISCORD LINK TEXT
local discordText = Instance.new("TextLabel", gui)
discordText.Name = "DiscordLink"
discordText.Size = UDim2.new(0, 400, 0, 30)
discordText.Position = UDim2.new(0.5, -200, 0.5, -15)
discordText.BackgroundTransparency = 1
discordText.Text = "BEST ANTI DESYNC: discord.gg/CKwk5M4SP4"
discordText.TextColor3 = WHITE
discordText.Font = Enum.Font.GothamBold
discordText.TextSize = 14
discordText.TextXAlignment = Enum.TextXAlignment.Center
discordText.TextYAlignment = Enum.TextYAlignment.Center
discordText.ZIndex = 5

do
    local dragging, dragInput, dragStart, mainStart = false, nil, nil, nil
    main.InputBegan:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
            dragging=true; dragStart=inp.Position; mainStart=main.Position
            inp.Changed:Connect(function()
                if inp.UserInputState==Enum.UserInputState.End then 
                    dragging=false
                    saveConfig() -- Save position when drag ends
                end
            end)
        end
    end)
    main.InputChanged:Connect(function(inp)
        if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then dragInput=inp end
    end)
    UIS.InputChanged:Connect(function(inp)
        if inp==dragInput and dragging then
            local dx = inp.Position.X - dragStart.X
            local dy = inp.Position.Y - dragStart.Y
            main.Position = UDim2.new(mainStart.X.Scale, mainStart.X.Offset+dx, mainStart.Y.Scale, mainStart.Y.Offset+dy)
        end
    end)
end

-- // HEADER
local header = Instance.new("Frame",main)
header.Size=UDim2.new(1,0,0,50); header.BackgroundColor3=Color3.fromRGB(12, 35, 85); header.BorderSizePixel=0; header.ZIndex=5
Instance.new("UICorner",header).CornerRadius=UDim.new(0,16)
local headerPatch = Instance.new("Frame",header)
headerPatch.Size=UDim2.new(1,0,0,16); headerPatch.Position=UDim2.new(0,0,1,-16)
headerPatch.BackgroundColor3=Color3.fromRGB(12, 35, 85); headerPatch.BorderSizePixel=0; headerPatch.ZIndex=4

local headerDiv = Instance.new("Frame",header)
headerDiv.Size=UDim2.new(1,0,0,2); headerDiv.Position=UDim2.new(0,0,1,-2)
headerDiv.BackgroundColor3=BORDER; headerDiv.BorderSizePixel=0; headerDiv.ZIndex=6

local titleLbl = Instance.new("TextLabel",header)
titleLbl.Size=UDim2.new(0,150,1,0); titleLbl.Position=UDim2.new(0,14,0,0)
titleLbl.BackgroundTransparency=1; titleLbl.Text="⚡ Ahmad Anti Desync"
titleLbl.TextColor3=BORDER; titleLbl.Font=Enum.Font.GothamBlack; titleLbl.TextSize=12
titleLbl.TextXAlignment=Enum.TextXAlignment.Left; titleLbl.ZIndex=6

-- Mobile/PC Toggle Button
local modeToggleBtn = Instance.new("TextButton", header)
modeToggleBtn.Size = UDim2.new(0, 50, 0, 28)
modeToggleBtn.Position = UDim2.new(1, -88, 0.5, -14)
modeToggleBtn.BackgroundColor3 = KB_BG
modeToggleBtn.BorderSizePixel = 0
modeToggleBtn.Text = State.mobileMode and "PC" or "MOBILE"
modeToggleBtn.TextColor3 = BORDER
modeToggleBtn.Font = Enum.Font.GothamBold
modeToggleBtn.TextSize = 8
modeToggleBtn.ZIndex = 10
Instance.new("UICorner", modeToggleBtn).CornerRadius = UDim.new(0, 6)
local modeToggleStroke = Instance.new("UIStroke", modeToggleBtn)
modeToggleStroke.Color = BORDER
modeToggleStroke.Thickness = 1.5

local closeBtn = Instance.new("TextButton",main)
closeBtn.Size=UDim2.new(0,28,0,28); closeBtn.Position=UDim2.new(1,-38,0,11)
closeBtn.BackgroundColor3=KB_BG; closeBtn.BorderSizePixel=0
closeBtn.Text="✕"; closeBtn.TextColor3=BORDER; closeBtn.Font=Enum.Font.GothamBlack
closeBtn.TextSize=14; closeBtn.ZIndex=10
Instance.new("UICorner",closeBtn).CornerRadius=UDim.new(0,8)
local closeBtnStroke = Instance.new("UIStroke",closeBtn); closeBtnStroke.Color=BORDER; closeBtnStroke.Thickness=1.5
closeBtn.MouseEnter:Connect(function()
    TweenService:Create(closeBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(35, 80, 180)}):Play()
end)
closeBtn.MouseLeave:Connect(function()
    TweenService:Create(closeBtn,TweenInfo.new(0.1),{BackgroundColor3=KB_BG}):Play()
end)
closeBtn.MouseButton1Click:Connect(function()
    State.autoBatToggled = false
    gui:Destroy()
end)

-- // CONTENT AREA
local content = Instance.new("Frame",main)
content.Size=UDim2.new(1,-20,1,-65); content.Position=UDim2.new(0,10,0,56)
content.BackgroundTransparency=1; content.BorderSizePixel=0

-- // AUTO BAT TOGGLE ROW
local toggleRow = Instance.new("Frame",content)
toggleRow.Size=UDim2.new(1,0,0,36); toggleRow.BackgroundColor3=CARD_BG; toggleRow.BorderSizePixel=0
Instance.new("UICorner",toggleRow).CornerRadius=UDim.new(0,10)
local toggleRowStroke = Instance.new("UIStroke",toggleRow); toggleRowStroke.Color=BORDER; toggleRowStroke.Thickness=1.5
local toggleLbl = Instance.new("TextLabel",toggleRow)
toggleLbl.Size=UDim2.new(0,100,1,0); toggleLbl.Position=UDim2.new(0,12,0,0)
toggleLbl.BackgroundTransparency=1; toggleLbl.Text="Anti Desync"; toggleLbl.TextColor3=WHITE
toggleLbl.Font=Enum.Font.GothamBold; toggleLbl.TextSize=12; toggleLbl.TextXAlignment=Enum.TextXAlignment.Left

-- Keybind button
local keyBtn = Instance.new("TextButton",toggleRow)
keyBtn.Size=UDim2.new(0,50,0,22); keyBtn.Position=UDim2.new(1,-102,0.5,-11)
keyBtn.BackgroundColor3=KB_BG; keyBtn.BorderSizePixel=0; keyBtn.Text=Keys.autoBat.Name
keyBtn.TextColor3=BORDER; keyBtn.Font=Enum.Font.GothamBold; keyBtn.TextSize=9; keyBtn.ZIndex=11
Instance.new("UICorner",keyBtn).CornerRadius=UDim.new(0,6)
local ks = Instance.new("UIStroke",keyBtn); ks.Color=BORDER; ks.Thickness=1.5
local kListening = false; local kConn

local function stopListen(key)
    kListening = false
    if kConn then 
        kConn:Disconnect()
        kConn = nil 
    end
    TweenService:Create(ks, TweenInfo.new(0.1), {Color = BORDER}):Play()
    keyBtn.TextColor3 = BORDER
    if key then 
        keyBtn.Text = key.Name
        Keys.autoBat = key
        saveConfig()
    end
end

keyBtn.MouseButton1Click:Connect(function()
    if kListening then 
        stopListen(nil)
        return 
    end
    
    kListening = true
    keyBtn.Text = "..."
    keyBtn.TextColor3 = BORDER2
    TweenService:Create(ks, TweenInfo.new(0.1), {Color = BORDER2}):Play()
    
    kConn = UIS.InputBegan:Connect(function(inp)
        if not kListening then return end
        
        if inp.UserInputType == Enum.UserInputType.Keyboard then
            if inp.KeyCode == Enum.KeyCode.Escape then 
                stopListen(nil)
                return 
            end
            Keys.autoBatType = "Keyboard"
            stopListen(inp.KeyCode)
        elseif inp.UserInputType == Enum.UserInputType.Gamepad1 then
            Keys.autoBatType = "Gamepad"
            stopListen(inp.KeyCode)
        end
    end)
end)

-- Toggle pill
local pillBg = Instance.new("Frame",toggleRow)
pillBg.Size=UDim2.new(0,40,0,22); pillBg.Position=UDim2.new(1,-48,0.5,-11)
pillBg.BackgroundColor3=KB_BG; pillBg.BorderSizePixel=0; pillBg.ZIndex=8
Instance.new("UICorner",pillBg).CornerRadius=UDim.new(0,11)
local pStroke = Instance.new("UIStroke",pillBg); pStroke.Color=BORDER2; pStroke.Thickness=1.5
local dot = Instance.new("Frame",pillBg)
dot.Size=UDim2.new(0,16,0,16); dot.Position=UDim2.new(0,3,0.5,-8)
dot.BackgroundColor3=BORDER2; dot.BorderSizePixel=0; dot.ZIndex=9
Instance.new("UICorner",dot).CornerRadius=UDim.new(0,5)

-- Mobile button (full width)
local mobileBtn = Instance.new("TextButton", toggleRow)
mobileBtn.Size = UDim2.new(1, -12, 1, -4)
mobileBtn.Position = UDim2.new(0, 6, 0, 2)
mobileBtn.BackgroundColor3 = CARD_BG
mobileBtn.BorderSizePixel = 0
mobileBtn.Text = "⚡ TAP TO TOGGLE"
mobileBtn.TextColor3 = BORDER
mobileBtn.Font = Enum.Font.GothamBold
mobileBtn.TextSize = 12
mobileBtn.Visible = State.mobileMode
mobileBtn.ZIndex = 12
Instance.new("UICorner", mobileBtn).CornerRadius = UDim.new(0, 8)
local mobileBtnStroke = Instance.new("UIStroke", mobileBtn)
mobileBtnStroke.Color = BORDER
mobileBtnStroke.Thickness = 1.5

local function setToggleVisual(on)
    TweenService:Create(pillBg,TweenInfo.new(0.18),{BackgroundColor3=on and BORDER or KB_BG}):Play()
    TweenService:Create(pStroke,TweenInfo.new(0.18),{Color=on and BORDER2 or BORDER}):Play()
    TweenService:Create(dot,TweenInfo.new(0.18,Enum.EasingStyle.Back),{
        Position=on and UDim2.new(1,-19,0.5,-8) or UDim2.new(0,3,0.5,-8),
        BackgroundColor3=on and WHITE or BORDER2
    }):Play()
    
    TweenService:Create(mobileBtn,TweenInfo.new(0.18),{BackgroundColor3=on and BORDER or CARD_BG}):Play()
    TweenService:Create(mobileBtnStroke,TweenInfo.new(0.18),{Color=on and WHITE or BORDER}):Play()
    mobileBtn.TextColor3 = on and BG or BORDER
end

local function updateMode()
    if State.mobileMode then
        toggleLbl.Visible = false
        keyBtn.Visible = false
        pillBg.Visible = false
        mobileBtn.Visible = true
        modeToggleBtn.Text = "PC"
    else
        toggleLbl.Visible = true
        keyBtn.Visible = true
        pillBg.Visible = true
        mobileBtn.Visible = false
        modeToggleBtn.Text = "MOBILE"
    end
end

updateMode()

-- Mode toggle button click
modeToggleBtn.MouseButton1Click:Connect(function()
    State.mobileMode = not State.mobileMode
    updateMode()
    saveConfig()
    
    TweenService:Create(modeToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(25, 60, 150)}):Play()
    task.wait(0.15)
    TweenService:Create(modeToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = KB_BG}):Play()
end)

-- Mobile button click - toggles on/off
mobileBtn.MouseButton1Click:Connect(function()
    State.autoBatToggled = not State.autoBatToggled
    setToggleVisual(State.autoBatToggled)
end)

local toggleClk = Instance.new("TextButton",toggleRow)
toggleClk.Size=UDim2.new(1,0,1,0); toggleClk.BackgroundTransparency=1; toggleClk.Text=""; toggleClk.ZIndex=6
toggleClk.MouseButton1Click:Connect(function()
    if State.mobileMode then return end
    State.autoBatToggled=not State.autoBatToggled
    setToggleVisual(State.autoBatToggled)
end)
toggleClk.MouseEnter:Connect(function() 
    if not State.mobileMode then
        TweenService:Create(toggleRow,TweenInfo.new(0.1),{BackgroundColor3=CARD_HOV}):Play() 
    end
end)
toggleClk.MouseLeave:Connect(function() 
    if not State.mobileMode then
        TweenService:Create(toggleRow,TweenInfo.new(0.1),{BackgroundColor3=CARD_BG}):Play() 
    end
end)

-- // BAT LOGIC
local function getBat()
    local char=LP.Character; if not char then return nil end
    local tool=char:FindFirstChild("Bat"); if tool then return tool end
    local bp2=LP:FindFirstChild("Backpack")
    if bp2 then tool=bp2:FindFirstChild("Bat"); if tool then tool.Parent=char; return tool end end
    return nil
end

local function tryHitBat()
    if State.hittingCooldown then return end; State.hittingCooldown=true
    pcall(function()
        local bat=getBat(); if bat then
            bat:Activate(); local ev=bat:FindFirstChildWhichIsA("RemoteEvent")
            if ev then ev:FireServer() end
        end
    end)
    task.delay(0.08, function() State.hittingCooldown=false end)
end

local function getClosestPlayer()
    if not hrp then return nil,math.huge end
    local cp,cd=nil,math.huge
    for _,p in pairs(Players:GetPlayers()) do
        if p~=LP and p.Character then
            local tr=p.Character:FindFirstChild("HumanoidRootPart")
            if tr then local d=(hrp.Position-tr.Position).Magnitude; if d<cd then cd=d; cp=p end end
        end
    end
    return cp,cd
end

-- // CHARACTER SETUP
local function setupChar(char)
    task.wait(0.1)
    h=char:WaitForChild("Humanoid",5); hrp=char:WaitForChild("HumanoidRootPart",5)
    if not h or not hrp then return end
end

LP.CharacterAdded:Connect(setupChar)
if LP.Character then task.spawn(function() setupChar(LP.Character) end) end

-- // BAT AIMBOT HEARTBEAT
RunService.Heartbeat:Connect(function()
    if not (State.autoBatToggled and h and hrp) then return end
    local target,dist=getClosestPlayer()
    if target and target.Character then
        local tr=target.Character:FindFirstChild("HumanoidRootPart")
        if tr then
            if sethiddenproperty then
                sethiddenproperty(hrp, "PhysicsRepRootPart", tr)
            end
            local targetPos = tr.Position + Vector3.new(0, 0.9, 0)
            if (hrp.Position - targetPos).Magnitude > 8 then
                hrp.CFrame = CFrame.new(targetPos)
            end
            local cam = workspace.CurrentCamera
            cam.CFrame = CFrame.new(cam.CFrame.Position, tr.Position)
            tryHitBat()
        end
    end
end)

-- // KEYBIND HANDLER (only active when NOT in mobile mode)
UIS.InputBegan:Connect(function(inp, gp)
    if State.mobileMode then return end
    if gp then return end
    
    if Keys.autoBatType == "Keyboard" and inp.UserInputType == Enum.UserInputType.Keyboard then
        if inp.KeyCode == Keys.autoBat then
            State.autoBatToggled = not State.autoBatToggled
            setToggleVisual(State.autoBatToggled)
        elseif inp.KeyCode == Keys.guiHide then
            State.guiVisible = not State.guiVisible
            main.Visible = State.guiVisible
        end
    elseif Keys.autoBatType == "Gamepad" and inp.UserInputType == Enum.UserInputType.Gamepad1 then
        if inp.KeyCode == Keys.autoBat then
            State.autoBatToggled = not State.autoBatToggled
            setToggleVisual(State.autoBatToggled)
        end
    end
end)

print("[Envy Auto Bat Desync] Blue Theme Loaded! ⚡")
